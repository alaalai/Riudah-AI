{% extends "layouts.master" %}

{% block content %}

    {# 1. Fixed Banner (Hero / Main Banner) #}
    {% component 'home.fixed-banner' %}

    {# 2. Store Features (Benefits) #}
    {% component 'home.store-features' %}

    {# 3. Photos Slider #}
    {% component 'home.photos-slider' %}

    {# 4. Fixed Products #}
    {% component 'home.fixed-products' %}

    {# 5. Products Slider #}
    {% component 'home.products-slider' %}

    {# 6. Featured Products Style 1 #}
    {% component 'home.featured-products-style1' %}

    {# 7. Featured Products Style 3 #}
    {% component 'home.featured-products-style3' %}

    {# 8. Latest Products #}
    {% component 'home.latest-products' %}

    {# 9. Testimonials #}
    {% component 'home.testimonials' %}

    {# 10. Youtube Section #}
    {% component 'home.youtube' %}

    {# 11. Parallax Background #}
    {% component 'home.parallax-background' %}

{% endblock %}

{% block scripts %}
    <script defer src="{{ 'home.js' | asset }}"></script>
{% endblock %}
