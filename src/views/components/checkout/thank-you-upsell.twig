{# 
  Component: Thank You Upsell
  Description: Special offer on thank you page.
  Placement: Thank You Page
#}
<div class="thank-you-upsell bg-white border border-dashed border-primary rounded-xl p-6 mt-8 text-center max-w-2xl mx-auto">
    <div class="inline-block bg-primary/10 text-primary px-3 py-1 rounded-full text-xs font-bold mb-4 animate-pulse">
        {{ trans('pages.thankyou.one_time_offer') }}
    </div>
    
    <h3 class="text-xl font-bold text-gray-900 mb-2">{{ trans('pages.thankyou.add_mystery_box') }}</h3>
    <p class="text-gray-500 text-sm mb-6">{{ trans('pages.thankyou.mystery_box_desc') }}</p>
    
    <div class="flex items-center justify-center gap-4 mb-6">
        <div class="text-gray-400 line-through font-bold">$50.00</div>
        <div class="text-3xl font-bold text-primary">$19.99</div>
    </div>
    
    <button class="bg-primary text-white px-8 py-3 rounded-lg font-bold hover:bg-primary-dark transition-colors shadow-lg w-full md:w-auto">
        {{ trans('pages.thankyou.add_to_order') }}
    </button>
    
    <div class="mt-4 text-xs text-gray-400">
        <i class="sicon-lock"></i> {{ trans('common.no_payment_needed') }}
    </div>
</div>
