{# 
  Component: Satisfaction Guarantee
  Description: Banner stating return policy/guarantee to reduce risk.
  Placement: Product Page (Sidebar or below description)
#}
<div class="satisfaction-guarantee bg-green-50 border border-green-100 rounded-lg p-4 my-6">
    <div class="flex items-start gap-3">
        <div class="w-10 h-10 bg-white rounded-full flex items-center justify-center shadow-sm text-green-500 shrink-0">
            <i class="sicon-check-circle text-xl"></i>
        </div>
        <div>
            <h4 class="font-bold text-gray-900 text-sm mb-1">{{ trans('pages.products.satisfaction_title') }}</h4>
            <p class="text-xs text-gray-600 leading-relaxed mb-2">
                {{ trans('pages.products.satisfaction_desc') }}
            </p>
            <ul class="space-y-1">
                <li class="flex items-center gap-2 text-xs text-gray-700">
                    <i class="sicon-check text-green-500"></i>
                    <span>{{ trans('common.money_back_guarantee') }}</span>
                </li>
                <li class="flex items-center gap-2 text-xs text-gray-700">
                    <i class="sicon-check text-green-500"></i>
                    <span>{{ trans('common.easy_returns') }}</span>
                </li>
            </ul>
        </div>
    </div>
</div>
